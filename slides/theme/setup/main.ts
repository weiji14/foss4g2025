import { defineAppSetup } from '@slidev/types'

export default defineAppSetup(({ app, router }) => {
  // You can add any Vue app configuration here
  // For example, registering global components or plugins
})
